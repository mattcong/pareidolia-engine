import "./ProcessingOverlay.css"

export const ProcessingOverlay = () => {
  return (
    <div className="processing-overlay">
      <span className="processing-overlay__label">detecting</span>
    </div>
  )
}
